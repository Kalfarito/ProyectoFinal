import React from 'react';
import PropTypes from 'prop-types';
import styles from './BorrarNota.module.css';

const BorrarNota = () => (
  <div className={styles.BorrarNota} data-testid="BorrarNota">
    BorrarNota Component
  </div>
);

BorrarNota.propTypes = {};

BorrarNota.defaultProps = {};

export default BorrarNota;
